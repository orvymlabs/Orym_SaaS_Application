# Apache rewrite rules for Next.js static export
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Redirect trailing slashes for directories
  RewriteRule ^$ index.html [L]

  # If the file exists, serve it
  RewriteCond %{REQUEST_FILENAME} -f
  RewriteRule ^ - [L]

  # If the directory exists, serve it
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Otherwise, serve index.html for client-side routing
  # But EXCLUDE static assets to avoid SyntaxError: Unexpected token '<'
  RewriteCond %{REQUEST_URI} !^/_next/static/.*$
  RewriteCond %{REQUEST_URI} !^/.*\.(js|css|png|jpg|jpeg|gif|ico|svg|json)$
  RewriteRule ^ index.html [L]
</IfModule>

# Disable ETags to prevent caching issues
<IfModule mod_headers.c>
  Header unset ETag
</IfModule>

# Enable compression
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/css application/javascript application/json
</IfModule>
