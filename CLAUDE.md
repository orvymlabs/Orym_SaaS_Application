## Debug the Actual Meta OAuth URL — Do Not Guess

We now have a Meta error saying:

"The domain of this URL isn't included in the app's domains"

The current Claude analysis mentions:

`https://staticxx.facebook.com`

IMPORTANT:

DO NOT add `staticxx.facebook.com` to Meta App Domains.

That is a Meta/Facebook internal domain and is not our application domain.

Our real production domains are:

Frontend:
`https://apps.orvym.com`

Backend:
`https://orym-saas-application.onrender.com`

I need you to find the EXACT URL/domain that our application is sending to Meta and identify why Meta is complaining.

### Do this now:

1. Inspect the complete frontend Embedded Signup implementation.
2. Inspect the backend `meta_oauth.py`.
3. Find every occurrence of:

   * `redirect_uri`
   * `oauth/access_token`
   * `FB.login`
   * `config_id`
   * `auth_type`
   * `domain`
   * `staticxx.facebook.com`
   * `facebook.com`
4. Log the EXACT outgoing OAuth/token-exchange request parameters, but NEVER log:

   * app secret
   * access token
   * full OAuth code
5. Specifically print:

   * exact `redirect_uri`
   * exact Meta endpoint
   * exact frontend origin
   * exact request URL being generated
6. Determine whether `staticxx.facebook.com` is:

   * actually being sent as redirect_uri,
   * being generated by the Facebook SDK internally,
   * only appearing inside Meta's error response,
   * or being incorrectly introduced by our code.

### Critical:

Do NOT fix this by adding:

`staticxx.facebook.com`

to App Domains.

Do NOT guess redirect_uri values.

Do NOT use multiple redirect_uri attempts.

Use the CURRENT official Meta Embedded Signup documentation to determine the correct redirect_uri behavior.

The final production configuration must use our actual app domain:

`https://apps.orvym.com`

and whatever exact OAuth redirect URI Meta's current Embedded Signup documentation requires.

Also verify Meta Dashboard:

* App Domains
* Valid OAuth Redirect URIs
* Embedded Signup configuration
* Config ID
* allowed domains

Finally tell me:

1. EXACT URL/domain Meta is rejecting
2. WHERE that URL came from
3. EXACT code line generating it
4. EXACT Meta Dashboard setting that needs changing, if any
5. Exact code change required

Do not declare it fixed until the actual outgoing request has been identified.
