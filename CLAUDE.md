I NEED YOU TO FIX ONE SPECIFIC PRODUCTION BUG ONLY:
META WHATSAPP EMBEDDED SIGNUP ERROR 100 / SUBCODE 36008.

DO NOT MODIFY ANYTHING ELSE IN THE ORVYM SAAS PLATFORM.

==================================================
PRODUCTION DETAILS
==================================================

Frontend:
https://apps.orvym.com

Backend:
https://orym-saas-application.onrender.com

Meta App ID:
3862862217342382

Meta WhatsApp Embedded Signup Configuration ID:
2432311603846818

Current Graph API version:
v26.0

==================================================
CURRENT WORKING PARTS
==================================================

The following parts are ALREADY WORKING and MUST NOT be unnecessarily changed:

1. Facebook JS SDK loads successfully.
2. Facebook SDK initializes with App ID 3862862217342382.
3. FB.login() opens the WhatsApp Embedded Signup flow.
4. Configuration ID 2432311603846818 is being used.
5. Meta returns:
   response.authResponse.code
6. Authorization code length is approximately 451 characters.
7. Frontend successfully sends the code to:
   POST /api/integrations/meta/oauth/callback
8. Render backend receives the request.
9. Backend reaches:
   https://graph.facebook.com/v26.0/oauth/access_token

DO NOT REBUILD THESE PARTS.

==================================================
CURRENT FAILURE
==================================================

Render logs show:

META OAUTH TOKEN EXCHANGE

App ID:
3862862217342382

Parameter names:
client_id
client_secret
code

redirect_uri included:
False

Code length:
451

Meta response:

HTTP 400

Error Code:
100

Error Subcode:
36008

Error Type:
OAuthException

Error Message:
"Error validating verification code. Please make sure your redirect_uri is identical to the one you used in the OAuth dialog request"

This is the ONLY issue we need to solve.

==================================================
IMPORTANT: DO NOT GUESS THE REDIRECT URI
==================================================

Do NOT simply add:

https://apps.orvym.com/dashboard/integrations/

Do NOT simply remove redirect_uri.

Do NOT randomly try different redirect_uri values.

The error means Meta is validating the authorization code against the OAuth flow context that created that code.

We need to determine EXACTLY what redirect URI / OAuth context Meta associated with the code generated by this Embedded Signup flow.

==================================================
STEP 1 — AUDIT BEFORE CHANGING CODE
==================================================

Inspect the existing Embedded Signup implementation.

Find:

- FB.init()
- FB.login()
- config_id
- response_type
- override_default_response_type
- extras
- WA_EMBEDDED_SIGNUP message listener
- response.authResponse.code
- backend callback endpoint
- Meta token exchange implementation
- redirect_uri construction
- any old Facebook OAuth implementation
- any duplicate callback/exchange logic

DO NOT MODIFY CODE YET.

First explain:

A. How the authorization code is generated.
B. What OAuth context Meta uses for this FB.login(config_id) flow.
C. Whether a redirect_uri is actually required during the token exchange for THIS exact Embedded Signup flow.
D. If required, what EXACT redirect URI Meta expects.
E. Whether the current backend exchange is missing it.
F. Whether the existing code is mixing normal Facebook OAuth with Embedded Signup.

==================================================
STEP 2 — VERIFY THE OFFICIAL EMBEDDED SIGNUP FLOW
==================================================

Use the CURRENT official Meta WhatsApp Embedded Signup documentation as the source of truth.

The frontend should use the official config_id pattern:

FB.login(fbLoginCallback, {
  config_id: '2432311603846818',
  response_type: 'code',
  override_default_response_type: true,
  extras: {
    setup: {},
  }
});

Do not replace this with a generic Facebook OAuth URL.

Do not create a separate normal Facebook login flow.

==================================================
STEP 3 — VERIFY SDK
==================================================

Facebook SDK initialization should remain equivalent to:

FB.init({
  appId: '3862862217342382',
  autoLogAppEvents: true,
  xfbml: true,
  version: 'v26.0'
});

Do not change the App ID.

Do not change the Configuration ID.

==================================================
STEP 4 — VERIFY WA_EMBEDDED_SIGNUP EVENT
==================================================

The message listener must correctly listen for:

WA_EMBEDDED_SIGNUP

and safely capture:

data.event
data.data.phone_number_id
data.data.waba_id
data.data.business_id

The authorization code comes from:

response.authResponse.code

Do not incorrectly expect WABA ID or phone_number_id to always be inside authResponse.

==================================================
STEP 5 — PREVENT DUPLICATE EXCHANGE
==================================================

This is extremely important.

The authorization code is single-use and short-lived.

Make sure:

ONE user click
=
ONE FB.login session
=
ONE authorization code processing
=
ONE backend exchange.

Do not allow:

- duplicate FB.login
- duplicate message listeners
- duplicate code exchange
- retrying the same code
- processing the same code from two different callbacks

Implement a safe processing guard.

For example:

isLaunching
isProcessingCode
processedCode

The exact implementation is up to you.

==================================================
STEP 6 — TRACE THE ACTUAL OAUTH CONTEXT
==================================================

This is the most important part.

Inspect the browser/network request generated when:

FB.login({
  config_id: '2432311603846818',
  response_type: 'code',
  override_default_response_type: true
})

is launched.

Determine whether Meta internally associates the authorization code with:

- a redirect URI
- Facebook OAuth callback URL
- app domain
- origin
- configuration callback
- another OAuth context

Do NOT assume the answer.

Find the actual value/context.

Then inspect the backend token exchange.

Compare:

META AUTHORIZATION CONTEXT
vs
META TOKEN EXCHANGE CONTEXT

They must match according to the current official Meta flow.

==================================================
STEP 7 — FIX ONLY THE EXCHANGE
==================================================

After identifying the actual mismatch, make the SMALLEST possible change required to fix it.

If redirect_uri is required:

Use the EXACT redirect URI expected by Meta.

Do NOT invent one.

Do NOT use the current page URL unless Meta's flow actually requires it.

If redirect_uri must NOT be sent:

Keep it omitted and identify what other OAuth context/configuration is mismatched.

If the problem is caused by mixing two OAuth implementations:

Separate the Embedded Signup token exchange from the normal OAuth implementation.

Do NOT modify the normal authentication/login system.

==================================================
STEP 8 — VERIFY META CONFIGURATION
==================================================

Verify that:

App ID:
3862862217342382

belongs to:

Configuration ID:
2432311603846818

Also verify the configuration is the actual Facebook Login for Business / WhatsApp Embedded Signup configuration used by the production frontend.

Verify production domain:

apps.orvym.com

is correctly configured in the relevant Meta Login / OAuth settings.

Do not create a new configuration unless the current configuration is proven invalid.

Do not change unrelated Meta settings.

==================================================
STEP 9 — VERIFY APP SECRET
==================================================

Verify the production Render App Secret belongs to:

App ID 3862862217342382

Do NOT print the secret.

Do NOT expose it in logs.

If the secret is incorrect, only fix the relevant production environment variable.

==================================================
STEP 10 — SAFE LOGGING
==================================================

Keep diagnostic logs for this integration only.

Log:

- App ID
- Configuration ID
- frontend origin
- whether code was received
- code length
- whether WABA ID was received
- whether phone number ID was received
- whether business ID was received
- duplicate-processing status
- Meta HTTP status
- Meta error code
- Meta error subcode
- Meta FB trace ID

NEVER log:

- client_secret
- full authorization code
- access_token

==================================================
STEP 11 — DO NOT HIDE THE ERROR
==================================================

Do not make the API return success while Meta actually rejects the code.

The fix is successful ONLY when:

Meta returns a valid access token
AND
the existing ORVYM WhatsApp onboarding flow continues successfully.

==================================================
STEP 12 — FINAL PRODUCTION TEST
==================================================

After the minimal fix, test from:

https://apps.orvym.com

Expected flow:

Connect WhatsApp
↓
FB.login()
↓
Meta Embedded Signup opens
↓
User completes onboarding
↓
WA_EMBEDDED_SIGNUP FINISH event
↓
authorization code received
↓
code processed exactly once
↓
backend receives code
↓
Meta token exchange succeeds
↓
access token returned
↓
WABA ID available
↓
phone number ID available
↓
existing ORVYM WhatsApp integration completes
↓
connected/success state shown

==================================================
STRICT SCOPE
==================================================

DO NOT TOUCH:

- ORVYM signup
- ORVYM login
- authentication
- user sessions
- dashboard
- database schema
- unrelated API routes
- other integrations
- existing WhatsApp messaging functionality
- UI unrelated to Embedded Signup
- subscription system
- any other SaaS functionality

ONLY FIX:

Meta WhatsApp Embedded Signup
authorization-code exchange
Error 100 / Subcode 36008.

==================================================
IMPORTANT FINAL INSTRUCTION
==================================================

Do NOT deploy a speculative fix.

First identify the exact reason Meta rejects the code.

Then make the smallest possible code/configuration change.

If the remaining issue is entirely inside Meta App Dashboard configuration, clearly state the exact Meta setting that is mismatched instead of changing unrelated application code.

The final result must be a REAL working production Embedded Signup integration, not a workaround that suppresses the 400 error.