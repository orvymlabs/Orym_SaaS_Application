I need you to FIX the WhatsApp Embedded Signup OAuth integration completely. Do not give me another diagnosis only. Inspect the code, implement the correct fix, build it, deploy it, and verify it.

This issue has been unresolved for 2 days, so DO NOT make assumptions and DO NOT repeat the previous redirect_uri changes.

PRODUCTION ARCHITECTURE

Frontend:
https://apps.orvym.com

Backend:
https://orym-saas-application.onrender.com

Meta App ID:
3862862217342382

WhatsApp Embedded Signup Config ID:
2432311603846818


CURRENT PRODUCTION ERROR

The frontend successfully launches WhatsApp Embedded Signup.

Current production console:

"Launching WhatsApp Embedded Signup"

App ID: 3862862217342382
Config ID: 2432311603846818

Then Meta sends:

"WhatsApp Embedded Signup Message Event"

with:

domain=apps.orvym.com
origin=https://apps.orvym.com/...
base_domain=apps.orvym.com
code=<exchangeable code>

The frontend successfully receives:

"Exchangeable token code received"

Code length: 451

Then:

"Exchanging authorization code for access token"

Then:

"redirect_uri: NOT INCLUDED (correct for Embedded Signup)"

The frontend sends:

POST https://orym-saas-application.onrender.com/api/integrations/meta/oauth/callback

Render responds:

HTTP 400

"Error validating verification code. Please make sure your redirect_uri is identical to the one you used in the OAuth dialog request"


IMPORTANT:

The exchangeable code is successfully generated by Meta.

Therefore DO NOT assume the Meta Embedded Signup popup itself is broken.

The failure happens during the backend exchange of the exchangeable code.

==================================================
PHASE 1 — INSPECT THE ACTUAL CODE
==================================================

Before changing anything, inspect the actual repository.

Read:

frontend/app/dashboard/integrations/page.tsx

backend/routers/integrations.py

backend/services/meta_oauth.py

Also inspect any other files involved in:

- Facebook SDK initialization
- Embedded Signup
- postMessage handling
- OAuth callback
- Meta Graph API requests
- environment variables
- Meta configuration

Do not rely on previous Claude answers.

==================================================
PHASE 2 — TRACE THE COMPLETE REQUEST
==================================================

Trace the exact flow:

Meta Embedded Signup
        ↓
postMessage
        ↓
frontend receives exchangeable code
        ↓
frontend POST request
        ↓
Render backend
        ↓
backend Meta API request
        ↓
Meta response

For EACH step, determine the exact data being used.

==================================================
PHASE 3 — FRONTEND
==================================================

Inspect the exact Embedded Signup implementation.

Determine:

1. Exact FB.login() options
2. config_id
3. response_type
4. override_default_response_type
5. extras
6. whether redirect_uri is passed to FB.login()
7. how the exchangeable code is extracted from postMessage
8. exact request body sent to backend

Show the exact frontend request body in the final report.

DO NOT automatically add redirect_uri.

Determine the correct behavior from the actual WhatsApp Embedded Signup flow.

==================================================
PHASE 4 — BACKEND
==================================================

Inspect:

backend/routers/integrations.py

Find:

/api/integrations/meta/oauth/callback

Determine:

- HTTP method
- request schema
- exact parameters received
- exact service function called

Then inspect:

backend/services/meta_oauth.py

Find the exact code exchange implementation.

I need the EXACT Meta endpoint and parameters.

Specifically determine whether the backend sends:

client_id
client_secret
code
redirect_uri

or other parameters.

DO NOT GUESS.

==================================================
PHASE 5 — CRITICAL: META EMBEDDED SIGNUP FLOW
==================================================

Verify the implementation against Meta's CURRENT official documentation for:

WhatsApp Embedded Signup
exchangeable authorization code
server-side code exchange

Determine the correct Meta endpoint and exact parameters for exchanging the exchangeable code received from Embedded Signup.

IMPORTANT:

Do not confuse:

A) normal Facebook Login OAuth authorization-code flow

with:

B) WhatsApp Embedded Signup exchangeable-code flow.

The current frontend is using:

config_id = 2432311603846818
response_type = code
override_default_response_type = true

and receiving an exchangeable code through the Embedded Signup message event.

Determine the correct server-side exchange flow for THIS exact setup.

==================================================
PHASE 6 — REDIRECT_URI
==================================================

This is the most important part.

DO NOT assume:

"redirect_uri is not included because Embedded Signup"

and DO NOT assume:

"https://apps.orvym.com/dashboard/integrations"

is the correct redirect URI.

Trace the actual implementation and Meta documentation.

Determine:

1. What redirect URI, if any, Meta uses during authorization.
2. What redirect URI, if any, must be sent during code exchange.
3. Whether the same redirect_uri must be sent to Meta during both steps.
4. Whether the exchangeable code flow uses a different mechanism.
5. Whether the current backend is accidentally using a normal OAuth token exchange endpoint/flow instead of the Embedded Signup exchange flow.

Do not make another speculative redirect_uri change.

==================================================
PHASE 7 — META APP CONFIGURATION
==================================================

After determining the ACTUAL correct redirect URI/domain behavior, tell me exactly what must be configured in the Meta App.

Only include entries that are actually required.

Check:

- App Domains
- Valid OAuth Redirect URIs
- Allowed Domains for JavaScript SDK
- Facebook Login for Business
- Embedded Signup configuration
- WhatsApp Business configuration

DO NOT add random domains.

DO NOT add the Render backend domain unless Meta's actual flow requires it.

==================================================
PHASE 8 — IMPLEMENT THE FIX
==================================================

After identifying the real cause, implement the correct fix.

Frontend:
- Use the correct Embedded Signup implementation.
- Remove obsolete/incorrect redirect_uri logic if necessary.
- Ensure the exchangeable code is sent correctly to backend.

Backend:
- Use the correct Meta endpoint for the Embedded Signup exchangeable code.
- Send exactly the parameters Meta requires.
- Do not send parameters that do not belong to this flow.
- Use the correct App ID and App Secret.
- Never expose App Secret or access tokens in logs.

==================================================
PHASE 9 — ADD SAFE DEBUG LOGGING
==================================================

Add temporary diagnostic logging to backend.

Log:

- Meta endpoint
- App ID
- code length
- parameter names
- whether redirect_uri is included
- redirect_uri value if included
- Meta HTTP response status
- Meta error code
- Meta error subcode
- Meta error message
- fbtrace_id

NEVER log:

- App Secret
- access token
- full exchangeable authorization code

==================================================
PHASE 10 — VERIFY LOCALLY
==================================================

Run:

- Python syntax/import checks
- backend tests if available
- frontend production build

The frontend build MUST succeed.

Then inspect the generated production JS bundle.

Verify that the generated bundle actually contains the new implementation.

Search the generated bundle for the old log:

"redirect_uri: NOT INCLUDED (correct for Embedded Signup)"

If that message is no longer correct, it MUST NOT remain in the production bundle.

Do not claim the fix is complete until the build proves the new code is actually included.

==================================================
PHASE 11 — DEPLOY
==================================================

Deploy the backend to Render.

Deploy the frontend to the actual production hosting:

https://apps.orvym.com

DO NOT mention Netlify.

Frontend is hosted on Hostinger.

Backend is hosted on Render.

Wait for both deployments to complete successfully.

==================================================
PHASE 12 — PRODUCTION TEST
==================================================

Perform a fresh production test.

IMPORTANT:

Do NOT reuse an old exchangeable code.

The Meta exchangeable code expires quickly.

Open:

https://apps.orvym.com/dashboard/integrations

Click Connect WhatsApp Business.

Complete Embedded Signup.

Generate a fresh exchangeable code.

Then inspect the frontend and Render logs.

The test is NOT considered successful merely because the popup opens.

The test is successful only if:

1. Meta Embedded Signup opens.
2. Exchangeable code is received.
3. Frontend sends it to backend.
4. Backend exchanges it with Meta successfully.
5. Meta returns the required token/data.
6. Backend successfully completes the WhatsApp onboarding.
7. No redirect_uri mismatch occurs.
8. No HTTP 400 occurs.

==================================================
FINAL REPORT
==================================================

When finished, give me a concise but exact report containing:

1. ROOT CAUSE
2. EXACT FIX
3. Exact frontend FB.login configuration
4. Exact frontend request body
5. Exact backend callback endpoint
6. Exact Meta endpoint used for code exchange
7. Exact Meta parameters sent
8. Exact redirect_uri behavior
9. Required Meta App Domains
10. Required OAuth Redirect URI(s), if any
11. Files changed
12. Frontend build result
13. Backend deployment result
14. Production test result

Most importantly:

DO NOT SAY "FIXED" UNTIL THE PRODUCTION TEST ACTUALLY SUCCEEDS.

If production test still fails, show me the exact Meta error response and continue debugging the actual request instead of making another assumption.